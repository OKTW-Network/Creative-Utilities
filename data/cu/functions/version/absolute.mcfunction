execute if score #new_x.*.*-* Version matches ..-1 run scoreboard players operation #new_x.*.*-* Version *= #-1 num
execute if score #new_*.x.*-* Version matches ..-1 run scoreboard players operation #new_*.x.*-* Version *= #-1 num
execute if score #new_*.*.x-* Version matches ..-1 run scoreboard players operation #new_*.*.x-* Version *= #-1 num
execute if score #new_*.*.*-x Version matches ..-1 run scoreboard players operation #new_*.*.*-x Version *= #-1 num

execute if score #currently_x.*.*-* Version matches ..-1 run scoreboard players operation #currently_x.*.*-* Version *= #-1 num
execute if score #currently_*.x.*-* Version matches ..-1 run scoreboard players operation #currently_*.x.*-* Version *= #-1 num
execute if score #currently_*.*.x-* Version matches ..-1 run scoreboard players operation #currently_*.*.x-* Version *= #-1 num
execute if score #currently_*.*.*-x Version matches ..-1 run scoreboard players operation #currently_*.*.*-x Version *= #-1 num
